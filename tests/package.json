{
  "name": "acumen-tests",
  "version": "3.7.0",
  "private": true,
  "description": "Test harness for Acumen — loads the single-file app into jsdom and exercises it.",
  "scripts": {
    "test": "node test-logic.js && node test-ui.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
